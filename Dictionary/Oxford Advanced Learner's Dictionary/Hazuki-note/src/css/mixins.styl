transition(property = all, duration = .2s, easing = ease, delay = 0s) {
    transition: property duration easing delay;
}

user-select(value) {
    -webkit-user-select: value;
    user-select: value;
}
